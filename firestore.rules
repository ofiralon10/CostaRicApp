rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /shared/{docId} {
      allow read, write: if request.auth != null;
    }
    match /fcm-tokens/{memberId} {
      allow read, write: if request.auth != null;
    }
  }
}
